const productFields = [
	'name',
	'image',
	'productName',
	'brand',
	'category',
	'model',
	'characteristics',
];

const userFields = ['name', 'email', 'password'];

export const requiredFields = {
	product: productFields,
	user: userFields,
};
